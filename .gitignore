.claude/settings.local.json

# Editor backups
*~

# Personal scratch dirs / drafts kept locally, not part of the release.
.sheal/
sandshell_the_story.md

# AGENTS.md is generated by `sandshell install-agent amp` (and gemini in
# project scope). Each developer can install it locally for their checkout;
# checking it in would conflict with downstream consumers' own AGENTS.md.
AGENTS.md
